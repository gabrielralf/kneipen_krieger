import 'package:flutter/material.dart';

class PriceSuggestionPage extends StatelessWidget {
	const PriceSuggestionPage({super.key});

	@override
	Widget build(BuildContext context) {
		return const ColoredBox(
			color: Colors.white,
			child: SafeArea(
				child: SizedBox.expand(),
			),
		);
	}
}
